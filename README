Ceci est mon projet git pour openclassrooms :
la création d'un repository pour partager mes fichier vimrc entre mes différentes terminaux.
En parti basé sur le repo https://github.com/nelstrom/dotfiles de Drew Neil

Installation:

    git clone git://github.com/GodefroyClair/dotvim.git ~/.vim

Create symlinks:

    ln -s ~/.vim/vimrc ~/.vimrc

Install vundle

git clone https://github.com/VundleVim/Vundle.vim.git ~/.vim/bundle/Vundle.vim

/* NOT NECESSRY WITH VUNDLE */
/* Switch to the `~/.vim` directory, and fetch submodules: */
/*     cd ~/.vim */
/*     git submodule init */
/*     git submodule update */

/*compile vimproc 4 mac & linux */
cd ~/.vim/bundle/vimproc/
make
